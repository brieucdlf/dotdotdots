# Identité pro — copier en work.gitconfig (gitignored).
# Chargé automatiquement par ~/.gitconfig pour tout repo sous ~/Work/.

[user]
  # email = prenom.nom@employeur.example
  # signingkey = ...

# [commit]
#   gpgsign = true
